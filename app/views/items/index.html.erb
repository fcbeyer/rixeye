<h1>Listing items</h1>

<table>
  <tr>
    <th>Whitelist</th>
    <th>Issue</th>
    <th>Comment</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @items.each do |item| %>
  <tr>
    <td><%= item.whitelist_id %></td>
    <td><%= item.issue %></td>
    <td><%= item.comment %></td>
    <td><%= link_to 'Show', item %></td>
    <td><%= link_to 'Edit', edit_item_path(item) %></td>
    <td><%= link_to 'Delete', item, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Item', new_project_whitelist_item_path %>
